<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="style_h1">
        <item name="android:textSize">@dimen/textSizeH1</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="style_h2">
        <item name="android:textSize">@dimen/textSizeH2</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@color/azure</item>
        <item name="android:layout_marginBottom">@dimen/marginCleaner</item>
    </style>

    <style name="style_h3">
        <item name="android:textStyle">bold</item>
        <item name="android:padding">3dp</item>
    </style>
</resources>
